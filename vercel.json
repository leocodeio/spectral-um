{
  "version": 2,
  "buildCommand": "pnpm install && pnpm prisma generate --schema=./db/prisma/schema.prisma && pnpm run build",
  "builds": [
    {
      "src": "api/index.ts",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "api/index.ts"
    }
  ],
  "env": {
    "NODE_ENV": "production"
  }
}
